// ===========================================
// Threat bounds
// ===========================================

/** Lowest global threat: Earth is calm. */
export const MIN_THREAT = 0;

/** Highest global threat: Earth is overrun and the campaign is lost (GDD §5.3). */
export const MAX_THREAT = 100;
