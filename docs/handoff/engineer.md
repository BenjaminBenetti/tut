# Engineer eng-3 — #751 defeat explanation

- Director explicitly assigned #751 to eng-3 on 2026-09-08 despite its medium tier. Branch: `fix/751-defeat-panel`, from main `b6928d0`. #842 merged in #843; the half-height engine work merged through #810, #812 and #815.
- #751's defect is the headline: threat 100 ends the campaign, while lost cities count infestation 100. The panel now says “Threat limit reached” and explains the threshold. Outcome rules, statistics and balance are preserved. Recommendation: ship factual copy; any city-loss ending or pacing retune needs a separate stated design target.
- The new regression computes the default-tuning boundary and mounts the real outcome in the screen: day 299 remains active; day 300 loses at threat 100 with zero cities lost. The existing browser flow asserts the corrected text and zero losses, repeated three times with CI settings. Typecheck, lint, 2,179 unit tests, build and seven simulations passed.
- Built-app before/after defeat frames and the byte-identical victory control are in `docs/design/diagnostics/751/`. Director judges the frames before Tech Lead merge. These are constructed outcome fixtures, not campaign-balance measurements.
- [Discussion #968](https://github.com/BenjaminBenetti/tut/discussions/968) is the standing-orders channel. The one bounded watch polls its exact GraphQL query every five minutes, plus the open issue board, current issue comments and this seat's PR events. Confirmed there in discussion comment 18355550. Work-scoped direction belongs in issue/PR threads.
- When a ticket finishes and no seat label remains, say so visibly in the issue thread. The Director now authorizes taking eligible unowned p1/p2 engineering work from the open board. Check existing claims before starting. One watch only, no cron; act on changes and re-arm when waiting. Report repeated quiet expiries to the Director rather than disappearing.
