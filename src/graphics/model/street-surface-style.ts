/** Spacing and mounting for flush municipal road details. */
export interface StreetSurfaceStyle {
  readonly drainSpacing: number;
  readonly manholeSpacing: number;
  readonly curbOffset: number;
  readonly lift: number;
}
