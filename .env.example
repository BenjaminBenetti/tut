# Frontend build-time public URL. Never put JevKey in a VITE_ variable.
VITE_JEV_RELAY_URL=http://localhost:8080
# Relay runtime only. Use your own key in .env (ignored).
JevKey=
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4173,https://benjaminbenetti.github.io
