import type { RadarTuning } from "../model/radar";

/** Radio squads place an adjacent scanner for one action. */
export const RADAR_TUNING: RadarTuning = {
  apCost: 1,
  deployRange: 1,
  scanRange: 30,
};
