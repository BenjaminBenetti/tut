# Engineer eng-3 — #996 capture reproducibility

- #751 merged in #972. Its threat-limit explanation is factual; outcome rules and balance did not change. Victory control remains byte-identical (`368d10e9…bba`). Older engine/fixture work is merged through #810/#812/#815/#843.
- Active #996 (p1/high, seat:eng-3), branch `fix/996-capture-reproducibility`, based on main `81c9c5d`. Two measured causes: separate browser keydown/keyup deliveries create held-key pan; unit count precedes map-art completion. The rig does not ease. Measurements and frames: `docs/design/diagnostics/996/`.
- `e2e/capture-frame.helper.ts` provides atomic DOM taps, a drawn-frame wait, and map+unit readiness. Both named layer specs use it. Gameplay camera behavior is preserved. The full capture also caught #978 hiding the tallest roof on the top no-op; the instance filter now honors the uncut sentinel, with roof/top/restore tests. New browser regressions compare exact PNGs across independent mounts/no-op and hold a real model response past unit readiness.
- Validation: typecheck/lint pass; 2,258 units pass with four workers (one existing skip). Initial unrestricted unit run timed out in the unchanged slope matrix under shared-host load 89. CI-shaped new browser tests: four passed, no retries/flakes. Build passes. Seven simulations pass. The roof correction passes the full 2,258-unit suite and the repeated browser gate (four passed, zero flaky); the final frame replay precedes PR.
- After #996 lands, hand the merged helper/head link to eng-5 for the owed #978 flat-map visual control. #961 needs its regenerated exact top/no-op pair too. #1003 supplies separate hillside evidence and may touch the same spec; preserve its intent when resolving any overlap.
- Discussion #968 is the standing-orders channel. One bounded watch polls its exact GraphQL query, the open issue board, work issue comments and this seat's PR events every five minutes. Work-scoped direction belongs in the issue/PR; report an empty queue visibly before waiting. No cron or second watcher; act and re-arm, and report repeated quiet expiries.
