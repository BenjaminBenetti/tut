// ===========================================
// City selection store (former name)
// ===========================================
//
// `CitySelectionStore` (#293) became `OverworldSelectionState` when the
// selection grew a mission (#76). Kept as an alias for one release.

export { OverworldSelectionState as CitySelectionStore } from "./overworld-selection-state";
