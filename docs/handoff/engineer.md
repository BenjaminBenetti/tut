# Engineer eng-3 — #1024 interior movement picking

- #751 merged in #972. Its threat-limit explanation is factual; outcome rules and balance did not change. Victory control remains byte-identical (`368d10e9…bba`). Older engine/fixture work is merged through #810/#812/#815/#843.
- #996 merged in #1013 (`086cab1`); capture readiness, atomic taps and top-floor roof fix are on main. #961's top/no-op evidence is settled; eng-5's independent #1019 control is accepted. Readiness alone does not reject asset fallback: that separate guard belongs to eng-4's #1021/#1031.
- Active #1024 (p2/high), branch `fix/1024-interior-movement-picking`, base `9d9ea01` (v0.2.15). Retired invisible placeholders and visible wall/roof CPU geometry intercepted highlighted floor clicks; fog also rejected the highlighted unexplored doorway. Visible-only picking was insufficient.
- The displayed move range now supplies logical tile-surface picking; ordinary geometry fallback excludes invisible meshes. The Director approved highlighted legally reachable unexplored destinations on #1024; ordinary unhighlighted fog remains refused under #761. Unit/spawner priority and simulation movement rules remain unchanged. This path is independent of the pointer-follow reveal removed by #1023.
- `e2e/interior-movement.spec.ts` uses a real seed-4242 mission, positioned starter units, normal vision and actual right clicks. Clean baseline moves to the wrong pavement tile twice; fixed code completes three legal interior clicks twice, zero retries/flakes, no browser/asset diagnostics. Videos, coordinates, inspected frames and byte-identical initial control are in `docs/design/diagnostics/1024/`.
- Validation: typecheck/lint/build pass; 2,266 units pass with four workers and one existing skip; simulation sweep passes 7/7 before and after. PR thread carries final head verification and review status. Only the Tech Lead merges.
- Discussion #968 is the standing-orders channel. One bounded watch polls its exact GraphQL query, the open issue board, work issue comments and this seat's PR events every five minutes. Work-scoped direction belongs in the issue/PR; report an empty queue visibly before waiting. No cron or second watcher; act and re-arm, and report repeated quiet expiries.
