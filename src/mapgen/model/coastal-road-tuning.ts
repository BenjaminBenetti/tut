/** Dimensions of the pedestrian destination at a shore-facing street end. */
export interface CoastalRoadTuning {
  /** Complete carriageway rows converted to pavement before the water. */
  readonly apronDepth: number;
}
