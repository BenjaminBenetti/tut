/** Presentation tuning for street furniture and natural prop silhouettes. */
export interface EnvironmentDetailStyle {
  readonly lampSpacing: number;
  readonly hydrantSpacing: number;
  readonly curbOffset: number;
  readonly vegetationMinWidth: number;
  readonly vegetationMaxWidth: number;
  readonly vegetationMinHeight: number;
  readonly vegetationMaxHeight: number;
}
